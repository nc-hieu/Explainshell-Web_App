import React from 'react';
const Dashboard = () =>{
    return(
      <div style={{color: 'var(--color-primary, #fbbf24)'}}>
        <h3>Trang Tổng quan (Dashboard)</h3>
        <h4>Tính năng hiện đang phát triển...</h4>
      </div>
    );
};

export default Dashboard;